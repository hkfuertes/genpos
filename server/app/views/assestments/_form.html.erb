<%= form_with(model: assestment) do |form| %>
  <% if assestment.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(assestment.errors.count, "error") %> prohibited this assestment from being saved:</h2>

      <ul>
        <% assestment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
